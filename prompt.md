# 開発指示書：2Dクライミング・詰めパズルゲーム（Webプロトタイプ）

> 最新の仕様は [GAME_SPEC.md](GAME_SPEC.md) を参照してください。このファイルは初期の開発指示書です。

Webブラウザ（Canvas 2D API）で動作する、2D縦スクロールのクライミング・詰めパズルゲームの最小試作版（PoC）を作成してください。
HTML / CSS / JavaScript を単一の `index.html` ファイルにまとめて出力してください。

---

## 1. コアゲームメカニクス

### 【操作と物理挙動】
- **操作対象:** プレイヤーキャラクターの「胴体（重心）」。
- **操作方法:** 
  - 画面上の胴体をドラッグ（スワイプ）して移動させる。
  - 胴体を動かしている間、四肢（右手・左手・右足・左足）は胴体の位置に応じて「最も自然な近くのホールド」へ自動的に吸着・追従する。
  - 吸着中のホールドが視覚的にハイライト表示される。
  - 指を離した（リリースした）瞬間、現在の姿勢で位置が確定し、**スタミナ（手数）が1減少**する。
- **可動域と制限:**
  - 胴体と各ホールドの間には最大可動距離（関節の限界）が存在する。
  - 限界を超える位置へドラッグすると胴体の移動が停止するか、警告（赤色点滅など）が表示される。

### 【リソースとゲームオーバー】
- **スタミナ（手数）:** ステージ開始時に初期値（例: 15）が付与される。
- **ゲームオーバー条件:** ゴールに到達する前にスタミナが0になる。
- **クリア条件:** キャラクターの手足いずれかが最上段の「ゴールホールド」に到達する。

---

## 2. プロシージャル・ステージ生成アルゴリズム

外部アセットを使わず、JavaScriptの算術処理でステージを自動生成してください。

1. **シード値による再現性:**
   - ステージ数（例: `Level 1`, `Level 2`...）を疑似乱数生成器（PRNG: 例: Mulberry32）のシード値として使用する。
   - 誰が遊んでも同じレベルなら同じマップ構造が生成されるようにする。

2. **メインルート（クリア保証ルート）の生成:**
   - 画面最下部（スタート）から最上部（ゴール）に向け、可動域半径 $R$ の円を連続して重なるように配置してパス（ライン）を作成する。
   - レベルが上がるにつれて、このパスの蛇行率（ジグザグ度合い）を大きくする。
   - パス上の各円の重なり領域内に、必ず1〜2個の「必須ホールド」を配置する。

3. **ノイズホールド（誤解法・分岐）の配置:**
   - メインルート外の空間に、一定密度のランダムなホールドを散布する。

---

## 3. 画面構成・UI仕様

- **キャンバスサイズ:** モバイル画面を想定した縦長比率（例: 400x700px 相当、レスポンシブ対応）。
- **カメラ追従:** 胴体のY座標に合わせて、画面が縦方向にスムーズにスクロールする。
- **UI表示:**
  - 画面上部固定: 現在のレベル（Level X）、残りスタミナ（Stamina: XX）
  - ゲームオーバー / クリア時: 簡単なモーダルまたはオーバーレイテキストと「NEXT STAGE」「RETRY」ボタン。

---

## 4. ビジュアル表現（軽量・シンプル）

- **背景:** シンプルな岩壁風のグリッドまたは無地。
- **ホールド:** 円形または角丸四角形。
  - 通常ホールド: 灰色 / 円形
  - スタートホールド: 緑色
  - ゴールホールド: 黄色（最上部）
- **プレイヤー:**
  - 胴体: 円形（ドラッグ可能なUIであることがわかる色）
  - 四肢: 胴体から各ホールドへ伸びるシンプルな線（Stick figure風）

---

## 5. 技術要件

- 外部ライブラリ（PhaserやThree.js等）は使用せず、**Vanilla JavaScript + HTML5 Canvas API** のみで実装すること。
- マウスドラッグおよびタッチイベント（`mousedown/mousemove/mouseup`, `touchstart/touchmove/touchend`）の両方に対応すること。

まずは動作確認ができる最小限のプロトタイプを構築してください。
