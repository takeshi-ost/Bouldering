# ASCENT — 最新ゲーム仕様書

本書は、初期の [prompt.md](prompt.md) と、その後の調整依頼を統合した仕様書です。記載する調整値・挙動は、[index.html](index.html) から起動する現行のJavaScript実装に対応します。今後の変更時は、本書と実装を併せて更新します。

## 1. ゲームの目的

限られた手数で壁を登り、黄色のゴールホールドへの到達を目指す、2Dクライミング・詰めパズルです。プレイヤーはフィールド上をスワイプし、動かす四肢と姿勢を決めます。1本だけを支点に固定し、他の3本を移動させて次の姿勢へ進みます。

ステージ1は想定5手の入門ステージです。ステージ2からは壁の左右端付近まで使う大きな蛇行を取り入れます。ホールドの混み具合だけで正解ルートが目立たないよう、壁全体の配置を密度評価で調整します。

## 2. 操作と1手の流れ

### 2.1 基本操作

マウスの左ボタンドラッグと、1本指のタッチドラッグに対応します。

1. ゲームフィールド内の任意の位置を押してドラッグを開始します。胴体に触れる必要はありません。胴体は開始位置からのスワイプ量に合わせて動き、タッチ位置へ瞬間移動しません。
2. 最初に5px以上引いた方向へ最も遠く進める四肢を、固定する支点として自動選択します。
3. 支点の1本は同じホールドを維持し、他の3本が胴体の移動に合わせて吸着し直します。
4. 全四肢が吸着した状態で離すと、姿勢を確定しスタミナを1消費します。

### 2.2 支点の選択

初期のスワイプ方向に沿って、肩・股関節からの長さ制限の範囲で胴体が進める距離を比較します。最も遠く進める支持点を選び、方向を変えてもリリースまで維持します。次のドラッグで支点を選び直します。5px未満の移動では支点選択や胴体移動を行いません。

### 2.3 吸着先の選択

移動する3本は、肩から100px／股関節から110px以内のホールドへ吸着します。通常ホールドや足との重複は許可しません。ゴールホールドだけは左右の手で共有できます。

各四肢の自然な位置（胴体から左手 −52/−68px、右手 +52/−68px、左足 −40/+78px、右足 +40/+78px）に届く候補を対象として、3本をまとめて割り当てます。吸着できる本数を優先し、その中で目標位置への距離の二乗の合計を最小化します。届くゴールには優先して吸着します。空きホールドが足りない四肢は未吸着になります。

### 2.4 確定とキャンセル

| リリース時の状態 | 処理 | スタミナ |
| --- | --- | --- |
| 四肢がすべて吸着している | 現在の姿勢を確定 | 1消費 |
| 移動中の四肢が未吸着（両手でのゴール到達を除く） | ドラッグ開始時の姿勢へ戻す | 消費しない |
| タッチ操作がキャンセルされた／ウィンドウがフォーカスを失った | ドラッグ開始時の姿勢へ戻す | 消費しない |

操作前後で4つの接触位置が同じ場合は、手数・スタミナを消費せず、胴体と四肢を操作前の姿勢へ戻します。左右の手足の割り当てが入れ替わっていても、接触位置の組が同じなら同様です。重複位置は個数も含めて比較します。

## 3. 姿勢と可動域

可動域と吸着判定には、肩から100px／股関節から110pxの長さ制限を使います。胴体中心から112pxの旧制限は適用しません。表示する可動域も同じ条件に従います。

範囲外へ引いた場合、移動方向に沿って到達可能な境界で胴体を止め、胴体の色と案内文で警告します。ドラッグ中は移動可能領域を薄い緑色で表示します。

肩・股関節は胴体の四隅です。腕は全長100px、脚は全長110pxの等長2節として、肘・膝の曲げ伸ばしで描画します。肘は左右それぞれ外側へ張り出す曲げ方を選びます。膝は2つの曲げ方のうち上側を優先し、高さが同じ場合は外側を選びます。完全に伸びた場合は直線です。両足先のY座標は両手先のY座標以上（画面上で同じ高さか下）とし、固定中を含め、吸着した四肢にこの条件を適用します。未吸着の四肢は肩・股関節から下へ垂れ、約2.6秒周期で左右に約4〜5px揺れます。揺れは描画だけに適用し、吸着判定には影響しません。自然な姿勢を作れない位置では吸着を確定できません。

## 4. 手数、クリア、失敗

- 姿勢確定ごとに、使用手数を1増やし、残りスタミナを1減らします。
- 確定時に、両手が同じゴールホールドに吸着していればクリアです。片手だけ、または足での到達はクリアになりません。両手でゴールを掴んだ場合は足が未吸着でもリリースでクリアします。
- ゴールに到達する前にスタミナが0になった場合はゲームオーバーです。
- 最後の1手でゴールへ到達した場合は、クリアを優先します。
- クリア後は次のステージへ進めます。リトライは同じシード・同じ配置で開始します。

想定手数は、現行の支点選択・吸着・両手ゴール条件で探索して得た手順の長さです。初期スタミナはその手数＋1手とします。ゴール優先の持ち替えを含め、現在の配置ではステージ1は4手／スタミナ5、ステージ2は10手／スタミナ11です。最短手数の保証はありません。後半の手数は配置に依存し、各レベルで単調増加する保証もありません。

## 5. ステージ生成

### 5.1 再現性とメインルート

レベル番号をMulberry32のシードとして使用します。同じ実装バージョン・同じレベル番号であれば、同じマップを生成します。

ステージ1は入門用の専用配置です。ステージ2以降では、横幅400pxの壁に対して、中心線が概ねX=40〜360pxを往復する大きな蛇行コースを生成します。

ホールド配置の基準は曲線に沿った距離です。40px間隔の基準位置を用い、必要な位置に支持点を置きます。この段階の経路は探索の案内として使います。その後、現行の1点固定・3本吸着ルールを呼び出して姿勢を探索し、両手ゴールに到達する手順を表示用ルートとして採用します。検証ルートで使わないホールドを壁全体から削除して再検証します。その後、同じ吸着手順を維持できる分岐のみを全体で最大4個追加します。分岐の連結成分は最大2個、対応するルート位置は隣接する2手以内です。別の分岐とは「四肢の最大長×2＋胴体の対角長」より離し、分岐だけで長距離を進めない配置にします。連続空間の全操作を網羅した2手復帰の保証ではありません。

ステージ全体の高さは最大1400pxに制限します。必要に応じてコースのY座標を縮小し、横方向の振れ幅を保ちながら、ゴールまでを2画面分に収めます。

### 5.2 分岐ホールド

メインルートの周囲を含め、壁全体に追加ホールドを配置します。通常ホールドの見た目は、参考ルート上・ルート外で共通です。

候補位置は乱数で少しずらした格子から生成します。追加ホールドは、既存のホールドから52px以上離れていることを条件とします。参考ルートの支持点は先に配置されるため、支持点同士がこの距離より近い場合があります。

参考ルートの操作時に吸着先がずれることを防ぐため、各手の吸着目標の付近には小さな除外領域を設けます。壁全体の配置後、次の密度評価によって追加ホールドを調整します。

## 6. 密度評価と収束処理

### 6.1 評価の基準

「ステージ全体の個数」と「1画面に見える個数」を分けて扱います。基準は、ステージ1を生成して測定した、1画面内の最大ホールド数です。基準値は生成設定から算出されます。ルート検証による削減後の個数は画面下部に別途表示します。

スクロール中の画面についても評価し、途中だけホールドが密集する状態を検出します。画面内個数は、ホールド中心が表示領域に含まれるかで数えます。

併せて、壁を20px間隔でサンプリングし、各点から最も近いホールドまでの距離を調べます。この値を使い、一部の領域だけが大きな空白になる削除を避けます。

### 6.2 反復調整

1. 配置後の全体個数、1画面内の最大個数、局所的な空白を測定します。
2. 最大個数がステージ1の基準以下なら終了します。
3. 参考ルートに必要なホールドを保護し、削除候補を追加ホールドから選びます。
4. 削除後に大きな空白ができる候補を除外します。
5. 画面内個数の超過を減らす効果と、周囲の混雑度を評価し、効果の高い候補を1つ削除します。
6. 再評価し、基準に収まるまで繰り返します。

局所的な空白の許容値は、ステージ1で測った最大空白距離の1.3倍と、可動域半径の0.7倍のうち、大きい値です。

反復回数の上限は、調整開始時の追加ホールド数です。条件を満たす削除候補がなくなった場合は停止し、画面に「制約により調整停止」と表示します。

最終配置の個数と1画面内の最大個数は、ルート検証による削減後に再集計します。画面には密度調整回数とルート検証での削減個数を表示します。

## 7. 表示と画面構成

### 7.1 プレイヤーと壁

| 要素 | 表示 |
| --- | --- |
| 胴体 | 緑色の縦長長方形。幅40×高さ72px |
| 操作判定 | 胴体の各辺に8pxの余裕を加えた44×64pxの領域 |
| 頭 | 胴体上部の円 |
| 四肢 | 胴体の四隅からホールドへ伸びる、関節付きの簡易的な線 |
| 操作中の四肢 | 黄土色で強調 |
| 固定中の四肢 | ドラッグ中に「固定」と表示 |
| 通常ホールド | 灰色の円 |
| スタートホールド | 緑色の円 |
| ゴールホールド | 黄色の円と「GOAL」表示 |
| 使用中のホールド | 外周をハイライト |
| 背景 | 淡い色のグリッド状の壁 |

### 7.2 カメラとUI

基本画面は400×700px相当の縦長比率です。表示サイズは端末に合わせて拡縮し、ゲーム内の距離・密度判定には同じ論理座標を使用します。

カメラは胴体のY座標を滑らかに追従します。ドラッグ中は、指と胴体の位置がずれないようカメラを固定します。ステージ全体が最大2画面分なので、カメラの移動量は最大700pxです。

上部にはレベル、残りスタミナ、使用手数、想定手数を表示します。下部には操作案内、警告、リスタートボタンを配置します。

クリア・失敗時は結果オーバーレイを表示します。クリア時は使用手数と残りスタミナを表示し、「NEXT STAGE」と「RETRY」を選べます。失敗時は「RETRY」を表示します。

### 7.3 デバッグ表示

参考ルートは初期状態で表示し、チェックボックスで切り替えます。

- 青い点線：胴体の参考移動経路。
- 線上の矢印：胴体が進む向き。
- 数字：手順。0は開始位置。
- 数字：参考コースの通過点。

青点線は現行操作でクリアを確認した胴体位置の列です。現在の胴体位置から次の番号へ向かう方向にスワイプします。実際の支点や吸着先は入力と姿勢に応じて自動選択されます。

密度デバッグ表示には、1画面内の最大個数、基準個数、全体個数、調整回数、制約により停止した場合の状態を表示します。

## 8. 技術構成

- `index.html` は画面の構造と起動用の読み込み設定を担当します。
- ゲーム処理は `js/` 配下のJavaScriptファイル、スタイルは `styles.css` に分離します。
- 調整値は `js/config.js` に集約し、ステージ生成・操作・描画を個別に変更できる構成にします。
- Vanilla JavaScriptとHTML5 Canvas 2D APIで実装します。
- 外部ライブラリ・外部画像などのアセットを使用しません。
- ブラウザで `index.html` を直接開いて実行できます。
- マウスイベントとタッチイベントに対応し、タッチ操作中のページスクロールを抑制します。
- 描画は `requestAnimationFrame` を使用します。描画解像度は端末のピクセル比に対応し、上限を2倍とします。

外部JavaScriptは通常のスクリプトとして `defer` 付きで順番に読み込みます。ローカルファイルを直接開く実行方法に対応し、開発サーバーやビルド処理は不要です。ファイルの役割と調整方法は [README.md](README.md) に記載します。

## 9. 依頼内容の反映履歴

| 調整依頼 | 最新仕様への反映 |
| --- | --- |
| 最初のステージを最小5手にする | ステージ1を想定5手の専用配置にする（1点固定方式では最短保証なし） |
| 一度に動く四肢を1つまでにする | 最新依頼により、1点固定・他3本移動に変更 |
| ステージごとに手数とパスの複雑さを増やす | ステージ2から横断を導入し、現行の手数上限まで増加させる |
| パスをデバッグ描画する | 参考コースと通過点番号を表示する |
| ステージ2から画面端まで大きく蛇行させる | 曲線上の距離に基づき、左右端付近まで使うコースを生成する |
| ルート上とそれ以外の密度を近づける | 壁全体への候補配置と、局所的な空白の評価を導入する |
| ゴールまでを2画面分以内にする | ステージ全体の高さを1400px以下に制限する |
| ホールドを約半分にする | 追加配置の間隔を52pxへ広げる |
| 胴体を縦長長方形、四肢を四隅から伸ばす | 40×72pxの胴体と四隅を起点とする描画にする |
| ステージ1の密度へ収束させる | 1画面内の最大個数を基準として反復調整する |
| 引いた方向の四肢を伸ばす | 最新方式では、進行方向に適した支点を自動選択する |
| HTML内の直書きをやめ、JavaScriptで調整しやすくする | 設定・生成・ゲーム処理・入力・描画を外部ファイルに分離し、`index.html` から起動する |

## 10. 動作確認の観点

今後の調整では、次の条件を確認します。

- スワイプ方向に適した支点を自動選択すること。
- 軸方向のドラッグ、微小なぶれ、壁際の入力で選択が安定すること。
- 1回のドラッグ中に、選ばれた1つの支持点が変化しないこと。
- 胴体の移動範囲と吸着距離が制限内に収まること。
- ステージ1を想定手数以内でクリアできること。
- 参考ルートが現行の1点固定操作でたどれること。
- 同一レベルの再生成で配置が再現されること。
- 最大2画面の高さ制限と、密度評価・停止条件を守ること。
- キャンセル、リトライ、スタミナ切れ、最後の1手でのクリアが正しく処理されること。
- マウス・タッチ双方で操作でき、描画や実行時のエラーがないこと。

検証結果は変更ごとに確認します。以前の3点固定方式での最短手数検証は、現在の方式には適用しません。

デバッグパスの初期状態はOFFです。チェックボックスで表示を切り替えられます。最新の配置方針では均一な密度より、検証ルートと短い分岐にホールドを絞ることを優先します。


## ゴール優先とコース閲覧

両手がゴールへ届き、両足も重複しないホールドへ届く場合は、通常の支点固定より両手ゴールを優先します。ゴールへの持ち替えでは、操作開始時の左右どちらかの足を同じホールドに維持することが必須です。その足を「固定」と表示します。固定可能な足がない場合は両手同時の持ち替えを行いません。遠いゴールへの瞬間移動は許可せず、移動先が現在の足支点から届く場合、または通常移動で到達した場合に適用します。

PCのトラックパッドの二本指スワイプ、スマートフォンの二本指スワイプで壁を上下に閲覧できます。閲覧は姿勢・手数・スタミナを変更せず、移動量を壁の範囲に制限します。二本指へ切り替えた場合は進行中のプレイヤー操作をキャンセルします。指が1本残ってもプレイヤー操作へ切り替えず、全部離すまで待ちます。通常のプレイヤー操作開始時は、カメラを胴体の標準表示位置に戻します。

パス表示トグルはタイトル横に置き、初期OFFを維持します。
