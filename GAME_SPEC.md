# ASCENT — ボルダリングパズル 詳細ゲーム仕様

更新日：2026-09-13  
対象：現在の `index.html`、`styles.css`、`js/` の実装

この文書を最新仕様の参照先とする。`prompt.md` は初期の開発指示書であり、試行錯誤の途中で撤回された仕様は本書の現行ルールに含めない。設計上の狙いと、実装が保証している条件は区別して記載する。

## 1. ゲームの概要

### 1.1 コンセプト

限られた手数でホールドを乗り継ぎ、両手で黄色のゴールを掴む、縦スクロールの2Dボルダリングパズル。

ページを開くと「従来コース」「難関コース」「検証コース」を選択する。選択後にLEVEL 1を開始し、次のステージとリトライでも選択した方式を維持する。「コース選択に戻る」で選び直すとLEVEL 1から再開する。プレイヤーは四肢を直接操作せず、スワイプで胴体を移動させる。四肢のうち1本を支点として固定し、残り3本が届くホールドに自動吸着する。姿勢を試しながら、少ない手数で登れる経路を探す。

### 1.2 基本ルール

- 1回のスワイプ中は、選択された1本の四肢が同じホールドを維持する。
- 他の3本は移動・吸着し直しが可能。常に3本すべてを変更する必要はない。
- 全四肢が吸着した状態で操作を終えると、姿勢を確定できる。
- 接触位置が変わる有効な確定は1手・スタミナ1を消費する。
- 操作前後の4つの接触位置が同じなら、消費せず操作前の姿勢に戻る。
- ゴールだけは左右の手で共有できる。
- 両手が同じゴールを掴み、両足も吸着した状態を確定するとクリア。
- スタミナがなくなる前にゴールできなければ失敗。

### 1.3 実行環境

- HTML、CSS、JavaScriptとCanvas 2D APIによるブラウザゲーム。
- 外部ライブラリ、ビルド、開発サーバーは不要。
- `index.html` を直接開いて実行できる。
- PCのマウス・トラックパッド、スマートフォンのタッチに対応。
- プレイ進行の保存、アカウント、通信、ランキング、音声は現時点では実装しない。

## 2. 画面と座標

| 項目 | 仕様 |
| --- | --- |
| 論理画面サイズ | 幅400 × 高さ700px |
| ステージ幅 | 400px |
| ステージ高さ | 700〜1400px |
| スクロール量 | 0〜ステージ高さ−700px。最大700px |
| 座標系 | Xは右向き、Yは下向きに増加 |
| 胴体中心の水平範囲 | X＝25〜375px |
| 胴体中心の垂直範囲 | Y＝80〜ステージ高さ−40px |
| 表示比率 | Canvasは4:7を保って画面幅に合わせる |
| 高解像度描画 | デバイスピクセル比に対応し、最大2倍 |
| 外枠 | ページ本体は最大幅420px |

「2画面分」はステージの全高を指す。開始位置からさらに2画面分スクロールできるという意味ではない。

## 3. プレイヤー操作

### 3.1 操作一覧

| 操作 | PC | スマートフォン |
| --- | --- | --- |
| プレイヤー移動 | フィールド上で左ドラッグ | フィールド上で1本指スワイプ |
| コース閲覧 | 右端のスクロール帯を左ドラッグ | 右端のスクロール帯を1本指でスワイプ |
| 操作確定 | 左ボタンを離す | 操作中の指を離す |
| 再挑戦 | RESTART／結果画面のリトライ | 同左 |
| 次のステージ | 成功画面のNEXT STAGE | 同左 |
| デバッグパス | タイトル横の「パス表示」 | 同左 |

フィールド上の任意の位置から移動操作を開始できる。胴体を直接触る必要はない。PCの右ドラッグによるコース閲覧は採用しない。

### 3.2 スワイプと胴体移動

1. フィールド上で操作を開始し、現在の胴体位置と四肢の接触先を記録する。
2. カメラをプレイヤーの標準表示位置に戻す。
3. 入力開始位置からの移動量を、胴体の開始位置に加算する。
4. 最初に論理座標で5px以上移動したとき、固定する四肢を選択する。
5. 可動域の範囲内で胴体を動かし、残りの四肢の吸着先を更新する。
6. 入力終了時に、確定・無消費での復帰・キャンセルを判定する。

タッチした位置へ胴体が瞬間移動することはない。5px未満の初期移動はぶれとして扱い、胴体を動かさない。

### 3.3 支点の選び方

操作開始時の4つの接触先それぞれについて、最初のスワイプ方向へ胴体が進める距離を求め、最も遠く進める四肢を固定支点に選ぶ。

- 距離は、その四肢の付け根からホールドまでの最大長に基づく。
- 到達距離の探索は24回の二分探索。
- 同じ評価値の場合は四肢の配列順で先のものを維持する。
- 選択後は指を離すまで固定する四肢とホールドを変えない。
- 途中でスワイプ方向を変えても支点は自動で切り替わらない。
- 左上への入力で左手を選ぶ、といった四象限による四肢選択は行わない。

### 3.4 可動域

固定ホールドの位置を `h`、胴体から四肢の付け根までのオフセットを `o`、四肢の長さを `L` とすると、胴体中心 `p` の条件は以下。

```text
distance(p + o, h) ≤ L
```

可動域は中心 `h − o`、半径 `L` の円内となる。画面端の制限も併用する。

範囲外へ引いた場合は移動線分上の到達可能な境界で止め、胴体色で警告する。移動中は可動域を薄緑色で表示する。

以前の「胴体中心から112px以内」という追加の吸着制限は使用しない。

## 4. 体型と四肢の描画

### 4.1 寸法

| 部位 | 起点・寸法 |
| --- | --- |
| 胴体 | 幅40 × 高さ72pxの縦長長方形 |
| 左肩 | 胴体中心からX−20、Y−36px |
| 右肩 | 胴体中心からX＋20、Y−36px |
| 左股関節 | 胴体中心からX−20、Y＋36px |
| 右股関節 | 胴体中心からX＋20、Y＋36px |
| 腕 | 左右とも全長100px。上腕50px＋前腕50px |
| 脚 | 左右とも全長110px。大腿55px＋下腿55px |

胴体は回転せず、常に縦向き。頭と首を胴体の上に描く。

### 4.2 肘・膝

関節は、付け根と手足先をつなぐ等長2節の形として計算する。届く範囲で肘・膝を曲げ、四肢の長さを引き伸ばさない。

- 肘：左は左側、右は右側へ張り出す曲げ方を選ぶ。
- 膝：可能な2つの曲げ方のうち、上側を優先する。
- 膝の高さが同じ場合は、胴体に対して外側を選ぶ。
- 完全に伸びた姿勢では直線になる。
- 「上側」は2つの解の比較であり、膝が常に股関節より上になる保証ではない。
- 胴体・四肢同士の衝突判定や厳密な人体の関節角制限は実装しない。

### 4.3 手足先の上下関係

吸着しているすべての足先は、吸着しているすべての手先と同じ高さか、それより下に置く。

```text
各足先のY ≥ 各手先のY
```

固定している四肢も判定に含む。未吸着の四肢はこの比較から除外する。手を必ず肩より上に、足を必ず股関節より下に置く、といった別の拘束はない。

### 4.4 未吸着時

ホールドが割り当てられない四肢は、その付け根から下に垂らし、わずかに揺らす。

- 垂らす長さ：四肢の全長の98%。
- 揺れ角：最大約0.045ラジアン。
- 周期：約2.6秒。
- 左右の揺れ幅：約4〜5px。
- 四肢ごとに位相を変える。
- 揺れは描画専用。胴体・支点・吸着・スタミナの計算に影響しない。

未吸着のまま離すと、操作前の姿勢へ戻る。落下アニメーションや物理的な落下は行わない。

## 5. ホールドと自動吸着

### 5.1 種類

| 種類 | 表示 | 用途 |
| --- | --- | --- |
| 開始ホールド | 緑系 | 初期姿勢の4つの接触先 |
| 通常ホールド | 灰色系 | 移動先・支持点 |
| ゴールホールド | 黄色、GOAL表示 | 両手で掴む到達目標 |

通常・開始ホールドの描画半径は8px、ゴールは10px。掴んでいるホールドには外周の強調表示を付ける。

ルート上の通常ホールドとルート外の通常ホールドは同じ見た目とする。

### 5.2 到達条件

ホールド中心が、肩から100px以内、または股関節から110px以内にあれば、その四肢の距離上の候補になる。

実際の吸着には以下も必要。

- 他の四肢との重複禁止を満たす。
- 手足先の上下条件を満たす。
- 操作中の固定支点を維持する。

距離上は届いても、これらの条件によって吸着できない場合がある。

### 5.3 自然な開き方の目標位置

動く四肢の吸着目標は、現在の胴体中心から以下のオフセットとする。

| 四肢 | X | Y |
| --- | ---: | ---: |
| 左手 | −52px | −68px |
| 右手 | ＋52px | −68px |
| 左足 | −40px | ＋78px |
| 右足 | ＋40px | ＋78px |

この目標位置は吸着候補の評価に使用する。手足先を必ずこの位置に固定するものではない。

### 5.4 割り当て

動かせる3本の候補をまとめて探索し、次の順で姿勢を選ぶ。

1. 重複・上下関係などの条件を満たす。
2. 吸着できる四肢の本数が多い。
3. 本数が同じなら、各目標位置からの距離の二乗の合計が小さい。

ゴール候補には評価値の優遇として `4 × 112²` を減算する。ただし距離・上下関係・重複・固定支点の条件を破って吸着することはない。同評価の候補はホールドID順などの固定された探索順に従う。

有効な組み合わせが成立しない場合は、その更新前の胴体位置を維持する。

### 5.5 ゴールの共有

- 通常・開始ホールドは、1個につき1本の四肢だけが使用できる。
- ゴールだけは、左右の手で同じホールドを共有できる。
- 片手が固定しているゴールにも、反対の手が通常の条件で届けば吸着できる。
- 足と手、または足同士でゴールを共有することはできない。
- 足が単独でゴールを使うことは距離等の条件次第で可能だが、それだけではクリアしない。

両手の共有は、吸着先の重複判定に対する例外のみ。胴体を強制移動したり、固定支点を切り替えたりする処理はない。また「必ず片手で確定してから次の手で反対の手を置く」という段階の強制もない。通常ルール内で両手が揃えば条件を満たす。

## 6. 確定、スタミナ、勝敗

### 6.1 入力終了時の優先順

| 状態 | 胴体・四肢 | 手数・スタミナ |
| --- | --- | --- |
| キャンセルされた | 操作前へ戻す | 変化なし |
| 1本でも未吸着 | 操作前へ戻す | 変化なし |
| 操作前後の4接触位置が同じ | 操作前へ戻す | 変化なし |
| 全四肢が吸着し、接触位置が変わった | 現在の姿勢を確定 | 1手加算・スタミナ1消費 |

確定後にゴール条件を確認し、未クリアの場合にスタミナ切れを判定する。

### 6.2 同じ接触位置への復帰

比較するのは四肢ごとの割り当てではなく、4つの接触座標の組。重複する座標は個数も含めて比較する。

例：

- 動かさずに押して離す：無消費。
- 途中で姿勢を変え、元の4点へ戻る：無消費。
- 元の4点のまま左右の手足が入れ替わる：無消費で、元の割り当てへ戻る。
- 胴体だけが違う位置に動き、4点は同じ：無消費で、胴体も元へ戻る。
- 1点以上の接触座標が変わり、全四肢が吸着している：1手消費。

### 6.3 クリア

全四肢が吸着し、有効な姿勢として確定したとき、左右の手が同じゴールホールドを掴んでいればクリア。

- 片手だけの到達ではクリアしない。
- 両手が揃っていても足が未吸着なら確定できない。
- 最後のスタミナ1を使って到達した場合は、失敗よりクリアを優先する。

### 6.4 スタミナと再挑戦

初期スタミナは、従来・難関コースでは検証済みルートの手数＋1、検証コースでは手数 M と同じ。操作時間やコース閲覧による消費はない。

成功後は次のレベルへ進める。失敗時・RESTARTでは同じレベルの配置を再生成する。手数、スタミナ、姿勢、カメラ閲覧状態を初期化する。パス表示のチェック状態は同じページ内では維持する。

## 7. カメラとコース閲覧

### 7.1 通常追従

胴体が画面の上から60%付近に見える位置を目標に、毎描画時に差分の8%ずつ追従する。壁の上端・下端で制限する。

プレイヤー操作中はカメラを固定し、入力と胴体の相対移動量がずれないようにする。新たに操作を始める際は標準位置へ即座に戻す。

### 7.2 右端のスクロール帯

ゲームフィールドの右側に幅32 CSS pxのスクロール帯を設ける。Canvasへ重ねず別の列として配置するため、帯がホールドやプレイヤーを隠すことはない。Canvasの論理座標400×700pxは変更しない。

- PCは帯を左ドラッグ、スマートフォンは帯を1本指でスワイプする。
- 帯のどこからでも相対移動を開始できる。押した位置へのジャンプは行わない。
- 帯を下へ動かすとカメラ座標が増加し、壁の下側へ移動する。上へ動かすと壁の上側へ移動する。
- カメラ移動量は「指の垂直移動量 × ステージ全高 ÷ 帯の表示高」とする。
- つまみの高さは画面高／ステージ全高、位置はカメラ座標／ステージ全高に対応する。
- Pointer Captureを使い、ドラッグ中に帯の外へ出ても追従する。
- 帯を操作した際は進行中のプレイヤー操作をキャンセルし、閲覧状態へ入る。
- 帯の操作・閲覧によって姿勢、手数、スタミナは変化しない。
- 帯から指を離しても閲覧位置を維持し、通常のプレイヤー操作を始めた時に標準表示位置へ戻る。
- 高さが1画面に収まるステージでは帯を薄く表示し、操作しない。
- 帯にキーボードフォーカスを置いた場合は、上下矢印で40px、PageUp／PageDownで1画面、Home／Endで壁の端へ移動できる。
- キャンセル、フォーカス喪失、リトライ時には帯の入力状態を解除する。

### 7.3 廃止した閲覧入力

フィールドの二本指スワイプと、PCのトラックパッド・ホイール入力によるステージスクロールは使用しない。

フィールドに2本目の指が触れた場合はプレイヤー操作をキャンセルし、全指を離すまで次のプレイヤー操作を受け付けない。二本指入力でカメラを動かすことはない。

## 8. ステージ生成

### 8.1 再現性とメインルート

レベル番号をMulberry32のシードとして使用します。同じ実装バージョン・同じレベル番号であれば、同じマップを生成します。

ステージ1は入門用の専用配置です。ステージ2以降では、横幅400pxの壁に対して、中心線が概ねX=40〜360pxを往復する折れ線コースを生成します。垂直な上昇と水平・下降する横移動を交互に配置し、角を曲線で補間しません。

ホールド配置の基準は折れ線に沿った距離です。40px間隔の基準位置を用い、必要な位置に支持点を置きます。この段階の経路は探索の案内として使います。その後、現行の1点固定・3本吸着ルールを呼び出して姿勢を探索し、ゴール到達に到達する手順を表示用ルートとして採用します。検証ルートで使わないホールドを壁全体から削除して再検証します。その後、同じ吸着手順を維持できる分岐のみを全体で最大4個追加します。分岐の連結成分は最大2個、対応するルート位置は隣接する2手以内です。別の分岐とは「四肢の最大長×2＋胴体の対角長」より離し、分岐だけで長距離を進めない配置にします。連続空間の全操作を網羅した2手復帰の保証ではありません。

ステージ全体の高さは最大1400pxに制限します。必要に応じてコースのY座標を縮小し、横方向の振れ幅を保ちながら、ゴールまでを2画面分に収めます。

### 8.2 分岐ホールド

メインルートの周囲を含め、壁全体に追加ホールドを配置します。通常ホールドの見た目は、参考ルート上・ルート外で共通です。

候補位置は乱数で少しずらした格子から生成します。追加ホールドは、既存のホールドから52px以上離れていることを条件とします。参考ルートの支持点は先に配置されるため、支持点同士がこの距離より近い場合があります。

参考ルートの操作時に吸着先がずれることを防ぐため、各手の吸着目標の付近には小さな除外領域を設けます。壁全体の配置後、次の密度評価によって追加ホールドを調整します。

### 8.3 密度評価と収束処理

#### 8.3.1 評価の基準

「ステージ全体の個数」と「1画面に見える個数」を分けて扱います。基準は、ステージ1を生成して測定した、1画面内の最大ホールド数です。基準値は生成設定から算出されます。ルート検証による削減後の個数は画面下部に別途表示します。

スクロール中の画面についても評価し、途中だけホールドが密集する状態を検出します。画面内個数は、ホールド中心が表示領域に含まれるかで数えます。

併せて、壁を20px間隔でサンプリングし、各点から最も近いホールドまでの距離を調べます。この値を使い、一部の領域だけが大きな空白になる削除を避けます。

#### 8.3.2 反復調整

1. 配置後の全体個数、1画面内の最大個数、局所的な空白を測定します。
2. 最大個数がステージ1の基準以下なら終了します。
3. 参考ルートに必要なホールドを保護し、削除候補を追加ホールドから選びます。
4. 削除後に大きな空白ができる候補を除外します。
5. 画面内個数の超過を減らす効果と、周囲の混雑度を評価し、効果の高い候補を1つ削除します。
6. 再評価し、基準に収まるまで繰り返します。

局所的な空白の許容値は、ステージ1で測った最大空白距離の1.3倍と、可動域半径の0.7倍のうち、大きい値です。

反復回数の上限は、調整開始時の追加ホールド数です。条件を満たす削除候補がなくなった場合は停止し、画面に「制約により調整停止」と表示します。

最終配置の個数と1画面内の最大個数は、ルート検証による削減後に再集計します。画面には密度調整回数とルート検証での削減個数を表示します。

## 9. 想定ルートの探索と検証

探索はゲーム本体と同じ支点選択・到達距離・吸着割り当て・両手ゴール判定を使用する。支点候補は全レベルで四肢の4本。

- 状態は胴体位置と4本の接触先。
- 初期経路の近傍と途中点、周囲の候補点を探索する。周辺候補はX方向−60／−30／0／30／60px、Y方向−90／−45／0px。LEVEL 2以降は下方向＋20／＋40pxも含める。
- 固定支点から届かない姿勢、未吸着、接触位置が変わらない手を除外する。
- 各深さで進行評価の高い最大12状態を残し、最大40手を探索する。
- 胴体位置を8px単位に丸めた値と接触先IDで訪問済み状態を整理する。
- 開始ホールドと解で使用するホールドを保護し、未使用ホールドを削除して再検証する。
- 短い分岐を最大4個戻す際も、ルート全体の吸着先が変わらないことを確認する。

表示するパスは検証済みの胴体の手順。従来・難関コースの初期スタミナは検証手数＋1手。検証コースは検証手数と同じ。従来コースのLEVEL 1は5手でクリア可能。LEVEL 2だけを最高難度にする設定や、水平区間・下降を必須とする採用条件は設けない。

候補を絞る探索であり、最短手数、解の唯一性、レベルごとの手数の単調増加は保証しない。`node tests/routes.cjs` でステージ1〜20の実操作処理によるルート再生、固定点、吸着重複、クリアとスタミナを確認する。

## 10. UIとデバッグ表示

### 10.1 常設UI

- タイトル：ASCENT、サブタイトル。
- タイトル横：「パス表示」チェックボックス。
- 現在レベル。
- STAMINA：残り手数。4以下で警告色。
- 使用手数／想定手数。
- 操作中の固定四肢にはCanvas内で「固定」を表示する。
- ゲームフィールド下部に重なる説明パネルは表示しない。
- START／GOALの凡例。
- RESTART。
- 右端のスクロール帯。下部の操作説明文は表示しない。
- 密度・削減情報。

### 10.2 結果画面

成功・失敗時は壁上に結果画面を重ねる。成功時はレベル、使用手数、残り手数を表示し、NEXT STAGEを選べる。リトライは同じレベルへ戻る。結果表示中はプレイヤー移動を受け付けない。

### 10.3 パス表示

初期状態はOFF。タイトル横でONにできる。

- 青い点線：検証済みの胴体経路。
- 矢印：経路の進行方向。
- 番号0：初期胴体位置。
- 番号1以降：各手の到達位置。
- カメラに合わせてスクロールする。
- 四肢選択用の斜め矢印は表示しない。

辿る場合は、現在の胴体から次の番号へ向かう方向にスワイプする。途中で方向を変えると支点・姿勢が想定と異なる場合がある。

### 10.4 密度情報

表示項目は、最終配置の最大個数／画面、初期密度基準、最終全体個数、中間工程の密度調整回数、調整停止状態、ルート検証での削減個数。

最終配置の全体個数と画面内最大個数は、ルート外削減後に再集計する。中間工程の調整回数・空白評価などとは計測段階が異なる。

## 11. 実装構成

| ファイル | 役割 |
| --- | --- |
| [index.html](index.html) | HTML、UI、スクリプトの読み込み順 |
| [styles.css](styles.css) | レイアウト、色、レスポンシブ表示 |
| [js/config.js](js/config.js) | 体型、生成、密度、入力、描画の定数 |
| [js/utils.js](js/utils.js) | 距離、制限、乱数、生成用の補助計算 |
| [js/stage.js](js/stage.js) | 蛇行コースの生成、密度評価と反復調整 |
| [js/game.js](js/game.js) | 状態、支点、吸着、確定、勝敗、UI更新 |
| [js/route.js](js/route.js) | ルート探索、再検証、不要ホールド削減、分岐 |
| [js/input.js](js/input.js) | プレイヤーのマウス・タッチ入力、帯のPointer Events・キー入力、ボタン |
| [js/renderer.js](js/renderer.js) | 体・四肢・ホールド・パス・カメラの描画 |
| [js/main.js](js/main.js) | 初期化と描画開始 |
| [tests/routes.cjs](tests/routes.cjs) | 接触・共有・配置・クリアの回帰テスト |
| [README.md](README.md) | 起動・調整・検証の案内 |
| [prompt.md](prompt.md) | 初期開発指示書。最新仕様ではない |

通常のスクリプトを `defer` で順番に読み込む。`file://` で動作させるため、ES Modulesの読み込みを前提にしない。

主な状態は胴体位置、操作開始時の胴体位置、現在と操作開始時の4接触先、固定四肢、レベル、壁高、ルート、スタミナ、手数、カメラ、閲覧状態、プレイ状態。プレイ状態は `playing`／`won`／`lost`。

`R = 112` は生成や吸着評価の係数として残っているが、現在の四肢の到達半径ではない。`TORSO.hitPadding` や四象限矢印の定数も、現在の入力操作を規定しない。

## 12. 検証と今後の変更時の確認項目

### 12.1 自動テスト

```sh
node tests/routes.cjs
```

主な検証対象：

- 同じ4接触位置なら無消費。
- 四肢が入れ替わった場合も元の姿勢へ戻る。
- 接触位置が変われば消費する。
- 重複接触の個数を正しく比較する。
- 左右どちらの手がゴールを固定していても反対の手を共有できる。
- 片手だけではクリアしない。
- 通常ホールドや足を含む共有は許可しない。
- ステージ1〜20で想定ルートを再実行できる。
- 各生成器が同一レベルを再現できる。
- LEVEL 2以降が画面幅を使った蛇行コースになる。
- 初期姿勢の全四肢が長さ制限内に収まる。
- スワイプを10分割して実行しても固定点が維持され、各通過点に到達する。
- 全高2画面、ルート外最大4個、分岐の連鎖制限を満たす。
- 想定手数で両手ゴールし、従来・難関コースでは余裕1手になる。検証コースは余裕0手。
- パスが初期OFF。

### 12.2 ブラウザで確認する項目

- フィールドの四隅・中央から同じ相対スワイプで動く。
- 開始時にコース選択が表示され、選択後にLEVEL 1が始まる。
- マウス・タッチでプレイヤーを操作できる。
- 帯のマウス・タッチ操作で姿勢や手数が変わらない。
- 帯の外へ指が移動してもドラッグが継続する。
- 閲覧後、通常操作開始時に標準位置へ戻る。
- 二本指入力でステージがスクロールしない。
- 下部の説明パネルがなく、帯がCanvasへ重なっていない。
- タイトル横のパストグルがスマホ幅で操作できる。
- 肘・膝の曲げ方向、未吸着の揺れ、固定表示が正しい。
- キャンセル、失敗、最後の1手での成功、リトライ、次ステージが機能する。

自動テストは列挙したケースの検証であり、すべての端末・全入力軌跡・全レベルの証明ではない。

## 13. 撤回済みの仕様

以下は現在のルールとして復活させない。

- 3本固定・1本だけ移動。
- 最初のスワイプの四象限による操作四肢の選択。
- 胴体中心から112pxでの吸着制限。
- ゴールへ届いたときの強制的な両手持ち替え。
- ゴール時だけの固定足の自動選択・途中切り替え。
- 直前姿勢を参照して固定足を特別に引き継ぐ処理。
- 足が未吸着でも両手ゴールだけで確定する例外。
- 四肢のいずれか1本だけのゴール接触によるクリア。
- 同じ4接触位置に戻った操作でのスタミナ消費。
- PCの右ドラッグによるコース閲覧。
- デバッグパスの初期ON。
- HTMLへのJavaScript・CSSの一括直書き。

ゴール共有のみは最新依頼で再導入済み。これは左右の手によるホールド共有の許可であり、固定支点や移動ルールの特例ではない。


## 追加仕様：難関コースの局所調整

従来コースの生成と操作ルールは維持する。難関コースは検証済み配置を土台に、ゴールに最も近い接触点以外の「特定の四肢＋ホールド」を突破の支点にする候補を探す。実装は `js/challenge.js`。

### 候補生成

- 中盤以降の各姿勢（パス2番から最終の一つ前）で100候補を検討する。
- 左右の手または足が使う通常ホールドを寄せ、位置を少しずらす。開始・ゴールは動かさない。
- 後半60候補では中間の胴体位置も各軸±30px内で調整する。
- 全ルートを通常操作で再生し、クリアできる候補のみ残す。新しい手順で使わないホールドを削除した後にも再検証する。

### 支点の条件

- 準備姿勢の4接触点からゴール中心までの直線距離を比較する。
- 出口へ向かう通常のスワイプで選ばれる支点は、最短距離の点より8px以上遠いこと。同率最短も正しく比較する。
- 最短距離の点を仮に固定した場合、出口の周囲±10pxの9点へ突破できないこと。これは検証専用の比較で、プレイヤーの固定点を強制する処理ではない。
- 通常操作では出口周辺9点のうち6点以上で、その鍵となる四肢を支点にして全四肢が吸着できること。
- 入口から出口周辺へ準備なしで直接抜けられる候補は除外する。
- 準備姿勢の周辺±40px・20px刻みの25点を調べ、鍵の「四肢＋ホールド」を使わず同じ2手で出口へ抜けられる候補は除外する。
- 準備位置の周辺±5pxの9点中6点以上で入口→準備→出口が成立すること。

手足の縮み量は候補の評価に残すが、縮むこと自体は必須条件としない。重要なのは鍵の1点を準備すること。残り3点の唯一性は要求しない。採用は1ステージに最大1区間。候補不成立なら従来配置を使う。

検証は固定された入口と有限の出口・中間候補について行う。全経路に対する唯一解・必須性や、最短手数の保証ではない。スタミナは従来同様、検証手数＋1手。結果は `densityStats.challenge.key` に四肢・ホールドID・最近点との距離差・出口の成功点数として記録する。

`node tests/routes.cjs` と `node tests/routes.cjs --challenge` で両方式のLEVEL 1〜20を操作再生する。


## 追加仕様：曲がり角の短い偽ルート

難関コースだけに、最大1か所の上向き分岐を追加する。主ルートで次の横移動が50px以上あり、直前より横方向の比率が増える地点を候補とする。LEVEL 1は対象外。

- 分岐は胴体が30〜120px上へ進む1手（横ずれは上昇量の75%以内）。追加する通常ホールドは1個で、他のホールドから35px以上離す。見た目は通常ホールドと共通。
- 分岐への移動で全四肢が吸着し、追加ホールドを使うことを検証する。
- その姿勢から元の分岐位置へ1手で戻り、元と同じ四肢の接触先に復帰できることを要求する。
- 枝の終端から上方向30〜150pxの格子候補（横ずれは上昇量の半分以内）を調べ、現在の最高接触点より10px以上高いホールドへ続けて進めないことを確認する。主ルートの次の1点へ横から戻ることは許容するが、その点を飛ばす移動は除外する。
- 主ルート全体の掴み先と、既存の鍵となる支点の評価が変わる追加は棄却する。
- 既存のルート外ホールドが4個ある場合も追加しない。成立しなければそのレベルには分岐を追加しない。判定は有限の候補点に対するもので、連続空間の全別解を排除する保証ではない。

「パス表示」をONにすると、主ルートの青色に加えて分岐を茶色の点線・終端の×で表示する。OFFでは双方を非表示にする。

スタミナの規則は維持する。枝への進入と復帰をそれぞれ確定すると合計2手を消費し、余裕1手を超える。未確定で元へ戻した場合は従来どおり消費しない。

曲線生成時点ではLEVEL 4・8・16・18に分岐が成立していた。折れ線生成への変更後は配置ごとに改めて検証する。


## 追加仕様：ゴール罠・トラバース・ホールド競合

難関コースにのみ適用する。従来の支点評価後、`js/puzzle-patterns.js` の局所パターン探索、短い上向き分岐、ゴール罠の順に評価する。各候補は主ルートの再生と既存の支点評価を通過した場合だけ採用する。採用できなければ元の配置へ戻す。

### ゴール直下の罠

`js/stage.js` がゴール直下45〜135px、横方向±60px以内に通常ホールド1個の候補を作る。既存ホールドから30px以上離す。正解ルートの残り2〜3手の姿勢から、そのホールドを掴み、全四肢が吸着して1手を確定できる候補を調べる。

`inspectSwipe()` は通常の支点選択・可動域クランプ・吸着・確定／巻き戻しを模擬する。ゴールへ向かう試行で両手の幾何学的な到達範囲内にゴールがあるにもかかわらず、足が未吸着で確定できず、未使用の足場が届かない、または手より上にしかない場合を足場失敗として記録する。固定手をゴールへ強制移動する特例は設けない。

残りスタミナは「検証手数＋1−分岐までの手数−罠へ入る1手」で算出する。復帰不能は、20pxの胴体領域セルと接触先の到達集合を使う保守的な判定で確認する。セル内で到達しうるホールドを広めに取り、途中の姿勢の対応関係・自動吸着の優先順位等の制約を緩める。実際より有利な条件でも残り手数内にゴールへ届かない場合に限り、手数不足を証明できた候補として採用する。届く可能性が残る場合は「不明」として棄却し、探索打ち切りを手詰まりと扱わない。

通常のビーム探索で解が見つからないだけでは採用しない。主ルート・既存の分岐・鍵となる支点が変わる追加も棄却する。採否は `densityStats.patterns.goalTrap` と `goalTrapRejected` に記録する。

### トラバース

既存の蛇行を土台に、目的姿勢の横50〜110pxに準備姿勢を挿入する。準備姿勢から目的姿勢への移動は同じY、または20px下降する。直進の支点となる通常ホールドを15〜30px下げ、準備を省いた直進ができない候補を探す。

その横移動の入口から上へ進む偽ルートが成立することも要求する。偽ルートは、先に依頼された「入るとすぐ手詰まりが分かる」1手の枝を維持する。2〜3手に延ばす案とは重複するため、スタミナへの影響が小さい既存方式を採用する。横移動の成立はルート再生と省略手順の局所比較で確認し、全経路の唯一性は保証しない。

### ホールド競合

既存パスに競合があれば再利用し、なければ足用の退避ホールド1個と、退避・手への持ち替えの姿勢を候補として生成する。始点で足が使う通常ホールドを中間姿勢では空け、次の姿勢では手が使うことを条件とする。

準備を省いた通常スワイプでは、その足が固定点に選ばれ、手足の共有禁止により有効な姿勢が成立しないことを確認する。比較として、そのホールドだけ手足で共有できると仮定すれば幾何学的に全四肢が成立することを調べる。共有許可は検証上の比較だけで、ゲーム本体のルールは変更しない。

退避ホールドは正解ルートに使用する場合だけ残す。新しい手順全体を再生し、既存難所の評価を保てない候補は棄却する。

### 回帰確認

両モードでLEVEL 1〜20を通常操作処理により再生する。加えて、人工配置で足の退避→手への持ち替えの成功、退避省略の失敗、ゴール試行時の足場失敗と無消費の巻き戻し、到達不能証明の肯定・否定を確認する。人工配置の成立と通常ステージでの採用数は区別する。


曲線生成時点のLEVEL 1〜20の検証結果：トラバースはLEVEL 14で採用（検証13手）、ホールド競合とゴール直下の罠は未採用。競合の人工配置では退避による成功と省略時の失敗を確認したが、通常ステージの全体クリア・既存難所を保つ生成候補は成立していない。ゴール直下では足場失敗を起こす候補もあったが、残り手数内の到達可能性を排除できず棄却した。生成器の追加と、実際に採用された配置を区別する。


## 現行の基準パス：折れ線

通常の2コースのLEVEL 2以降は、垂直上昇と横断を組み合わせる。上昇区間の高さは `climbHeight`（290px）＋シード付き変動 `climbVariation`（最大20px）。横断のX幅は320px、横断終点は始点より `traverseDrop`（20px）下げる。高さ上限に応じたY縮小後も、横断の水平以下という関係を維持する。

`isTraverse()` は横方向30px以上、かつ終点のYが始点以上の場合を横移動と判定する。斜め上への移動は含めない。検証済みのクリアルートにも、LEVEL 2以降では少なくとも1手の水平または下降する横移動を要求する。難関側の再配置後も同じ条件を維持する。分岐の入口は上昇から横移動への切り替わりを対象とする。

## 検証コース：5種類の制約パターン

検証コースは次の制約を1ステージずつ、疎な固定配置で確認する。支点選択、可動域、自動吸着、足と手の上下関係、通常ホールドの共有禁止、両手ゴール共有は通常と同じ。初期スタミナは想定ルートの手数 M と等しく、余裕は0手。ステージ5をクリアするとステージ1に戻る。

| ステージ | 難関箇所 | 配置と局所的な確認 |
| --- | --- | --- |
| 1 | ストレッチ | 離れた足軸を固定し、移動前に届かない手ホールドへ身体を斜めに伸ばして届く。2手。 |
| 2 | 近接経由 | 手足の距離が近い中間姿勢を経由し、そこにある足場を次の上昇の支点とする。3手。 |
| 3 | 最下部足場の強制選択 | 高さの近いホールド群の最下部を足場に選ぶ。保守的な接触グラフでも、その足場が最短経路の固定支点になる。3手。 |
| 4 | ぎりぎり届かない偽コース | ゴール方向の偽枝は確定できるが、次のホールドは手の可動域を3px超える。枝から残り手数内のゴール到達も保守的に否定する。3手。 |
| 5 | 逆方向への移動 | 最初に左下へ動き、低い手の配置から上昇支点へつなぐ。最短手数の下限は5手。 |

`js/verification.js` で各配置を作り、想定ルートを通常操作で再生する。最終配置には初期姿勢・想定ルートで使用するホールドと、ステージ4の偽枝として明示的に使用するホールドだけを残す。コース外の無作為なホールドは配置しない。ルート表示をONにした場合は、難関区間を矩形枠で囲む。

`tests/pattern-stages.cjs` は5ステージの通常入力によるクリア、M+0のスタミナ、難関箇所の局所条件、余分なホールドの不在、矩形枠の描画を確認する。保守的な接触グラフで1〜4の最短手数と固定支点列、5の最短手数を検証する。ステージ5は座標走査で前進する初手について、自動ソルバーによる残り手数内の解と表示ルートの残りへの接続がないことも確認する。全ての連続入力に対して胴体座標まで唯一になる保証ではない。従来・難関コース各20レベルは `tests/routes.cjs` で別途確認する。


## 開始時のカメラ演出とクリア後の実走パス

各ステージ開始・リトライ時はゴールを含む位置を450ms表示し、1200msかけてプレイヤーの通常表示位置までスクロールする。演出中はプレイヤー操作とスクロール帯を受け付けず、終了後に操作可能になる。壁が1画面に収まる場合はカメラ移動なしで同じ待機時間を使う。コース選択画面では演出を進めない。

開始位置と各手で確定した胴体位置だけをメモリ内の `playerPath` に一時記録する。スワイプ中の途中座標は記録せず、有効な移動の確定時に1点を追加する。点数は確定手数＋1となる。キャンセル、未吸着、同じ4接触位置への復帰は破棄する。再開始・次ステージでは初期化し、永続保存はしない。

クリア状態で「想定パスを表示」がONの場合のみ、青い想定ルートにピンクの実走パスを重ねる。想定ルートと同様、確定点を結ぶ点線の折れ線と進行方向の矢印、手順番号で表示する。クリア後のON/OFFにも対応する。比較中の結果パネルは小さく表示し、背景のぼかしを解除する。右端のスクロール帯でコース全体を閲覧できる。失敗時とプレイ中は実走パスを表示しない。
