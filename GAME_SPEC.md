# ASCENT — 現行ゲーム仕様

更新日：2026-09-20。旧「難関コース」の2肢固定ルールを両コースに採用する。起動時に「既存コース」「試作コース」を選択できる。1肢固定と固定パターン検証ステージは存在しない。

## 1. 起動と進行

`index.html` を開くとコース選択画面を表示する。選択後にそのコースのLevel 1を生成する。プレイ中のコース選択ボタンは廃止し、同じ場所にUndoを表示する。開始時のコース選択は維持する。開始時はゴールを450ms表示し、1200msでプレイヤーの通常表示位置へ移る。演出中は移動・スクロール入力を受け付けない。1画面に収まる壁も同じ時間待機する。

クリア後の「NEXT STAGE」で選択中のコースの次のレベルへ進む。「RETRY」「RESTART」は現在のレベルを再生成する。生成はレベル番号をシードに使い、同じ設定とレベルでは同じ配置になる。

開始用の足ホールドは左右別々で、ステージ下端から50pxの同じ高さに置く。手ホールドは開始胴体の中央、中心から68px上（肩より32px上）にある横長ホールド1個で、左右の手が共有する。全肢が到達できることを経路探索の初期状態で検証する。開始用の3ホールドは削減しない。通常の追加候補は足元より24px以上上へ制限し、足元2個を最下段として保つ。

## 2. 操作と2点固定

胴体に触れていなくても、フィールド上の任意の位置からマウス左ドラッグまたは1本指スワイプで胴体を移動できる。5px以上の初期移動で固定する2肢を選ぶ。

支持ペアの候補は、両手・両足・同じ側の手足2組・対角の手足2組の計6組。

1. 初期スワイプ方向について、それぞれのペアを固定したまま胴体を連続して動かせる距離を調べる。到達円・高い足の内側禁止円・高さ境界との交点で移動区間を分割し、最初に不成立になる境界までを可動距離とする。
2. その距離の25%、50%、75%、100%地点で、残る2肢に前方の新しいホールド候補があるか調べる。
3. 前方候補がある可動肢の数を優先する。同数なら胴体の移動可能距離が長いペアを選ぶ。完全な同点は候補列挙順で決まる。
4. 選んだ2肢とその接触先はスワイプ終了まで変更しない。

胴体が動ける範囲は、2支持点の到達距離と足の高さ・膝角度条件をすべて満たす範囲である。固定した足が股関節より上になる領域には内側の禁止領域が生じるため、移動先だけでなく途中の区間も判定する。入力は直前の有効位置から最初の境界までに制限する。入力方向を途中で変えても支持ペアは変わらない。

移動する2肢は自動吸着する。必ず2肢とも接触先を変える必要はない。固定ペア以外が偶然同じホールドに留まることは許される。

## 3. 可動域と吸着

論理画面は400×700px。壁の高さは最大1400px。胴体は幅40・高さ72pxの長方形で、四隅の肩・腰から四肢が伸びる。腕の長さは100px、脚は110px。到達判定は胴体中心ではなく各四肢の付け根を基準とする。

自然な手足先の目標位置は、胴体中心から左手(-52,-68)、右手(52,-68)、左足(-40,78)、右足(40,78)。吸着は到達可能なホールドの組み合わせを調べ、吸着する本数を最大化したうえで目標位置からの距離の二乗を最小化する。ゴールには吸着を優先する評価値を加える。

- 通常ホールドは1個につき1肢のみ。
- 横長ホールドは通常の2倍の横幅の長方形（32px、高さ16px）。影・使用中の枠・凡例も長方形で表示する。1肢、または両手、または両足で使える。手と足の混在および3肢以上の共有は禁止。同じホールド上の2肢を支持ペアにもできる。
- 横長ホールドの接触座標は中心の同一点。幅は表示上の形状であり、到達距離は拡大しない。
- ゴールは従来どおり左右の手で共有できる。両足・手足混在での共有はできない。
- 足先はボディの中点（胴体中心Y）以下まで。
- 足先が股関節より高い場合だけ、固定長の脚で膝の内角150度以上を必要とする。股関節〜足先の距離は約106.25〜110pxとなる。股関節と同じ高さ以下ならこの角度条件を課さない。
- これらの条件を吸着候補・固定足・支持ペア選択・ルート探索と再生に共通適用する。腕の到達距離と吸着評価は変更しない。
- 足先のYは、吸着したすべての手先のY以上とする。画面のYは下向きが正。
- 固定された2肢のホールドは占有されたまま維持する。
- 吸着先がない可動肢は垂れ下がり、描画上わずかに揺れる。

## 4. 確定と勝敗

入力終了時、以下は操作前の胴体・四肢配置へ戻し、スタミナを消費しない。

- キャンセル。
- 吸着していない四肢がある。
- 正しい2点固定が維持されていない。
- 四肢の接触位置の集合が操作前と同じ（四肢の割り当てが変わっていても戻す）。

それ以外は1手確定し、スタミナを1消費する。両手が同じゴールを掴み、両足も吸着していればクリア。ゴール判定をスタミナ切れより先に行うため、残り0になった手でのクリアも可能。ゴールを掴ませるために固定点を切り替える特例はない。

初期スタミナは検証済みルートの手数＋1。検証手数は最短手数の保証ではない。

### Undo

フッターのUndoは直前の有効な確定移動を1回取り消す。移動前の論理胴体座標・4接触・描画用の調整済み胴体位置・関節履歴・手数・スタミナ・実走パスを保存し、取り消し時に復元する。手数は1戻り、スタミナも1回復する。コースと想定ルートは変更しない。

履歴は1件のみ。Undo後の連続Undoは不可で、新しく有効な移動を確定すると再び利用できる。キャンセルや接触先が変わらない操作は履歴を置き換えない。ドラッグ中・開始演出中・開始時の選択画面では利用不可。クリア・手数切れ直後も利用でき、結果画面を閉じてプレイへ戻る。リトライ・次ステージで履歴を破棄する。

## 5. コース生成

詳細は [docs/course-generation.md](docs/course-generation.md) に記載する。

既存コースは従来どおり `reset()` → `prepareStage()` の生成経路を使う。

1. `stage.js` がレベルシードから初期ホールドとガイドを作る。両コース共通で開始時は、両足を最下段の独立した2個、両手を共有の横長ホールド1個へ配置する。
2. 初期密度をレベル1基準で調整する。
3. `solveRoute()` が実操作と同じ6支持ペア評価・2点固定・自動吸着・ボディ中点までの足先制限・高い足の150度条件でクリア手順を探索する。初期姿勢も同じ到達判定で検証し、各手の移動区間を通過できることも確認する。
4. `pruneUnusedHolds()` がルート未使用のホールドを1個ずつ仮削除する。
5. 全手の支点ペアと接触先、手順数を維持できる削除だけを採用する。
6. 削除がなくなるまで繰り返し、最終ルートと密度を確定する。

レベル1は直線ガイド、レベル2以降は垂直上昇と水平以下の横断を組み合わせた折れ線ガイドを使う。レベル2以降の検証ルートには横移動を少なくとも1手含める。

掴まないホールドでも支点選択の前方候補評価に必要な場合があるため、未使用という理由だけでは削除しない。残したIDは `densityStats.selectionHoldIds` に記録する。新たな飾り・偽枝・ゴール罠は追加しない。

旧1点固定向けの生成後調整と検証用逆算生成は削除済み。初期配置のガイドは探索候補を提供するものであり、固定された四肢移動の指定ではない。

### 試作コース

`reset()` → `preparePrototypeStage()` の独立した生成経路を使う。`stageGenerator.generate(level, true)` で既存と同一の初期ガイド・初期ホールド・ゴール・壁高を取得し、壁全体への追加と密度調整を省く。

ガイド上を28〜56pxの不均等な間隔で進み、各区間の終点も含めて自然な四肢目標位置へ候補を追加する。候補には左右最大16px・上下最大18pxの独立した揺らぎを加え、既存ホールドと24px以上離す。初期ガイド由来の通常ホールドにも左右最大14px・上下最大16pxの揺らぎを加える。画面端では範囲内から乱数を選び、座標の切り詰めによる整列を避ける。開始3ホールド・ゴール・初期ガイド・可動域は変更しない。専用のレベルシード乱数を使用し、リトライ時の配置は再現する。`solveRoute()` で現行の2点固定ルールによる通過ルートを求め、以後は胴体の確定位置列と手数を維持する。

スタート3個とゴールを除く全ホールドを1個ずつ仮削除し、`replayRoute()` で全手が成立する削除だけを採用する。既存側と異なり、支持ペアと接触先の変更を許す。削除がなくなるまで繰り返す。残った通常ホールドを1個でも削ると同じ胴体ルートを再生できない、という意味での必要最低限であり、全配置の最少個数の証明ではない。支持ペア選択のために必要な未接触ホールドは残る。

初期ガイドは共通だが、探索後の実際のルート・手数は既存と一致するとは限らない。検証は `node tests/prototype.cjs` で、20レベルの通常入力クリア、各ホールドの必要性、初期ガイドの一致、リトライ、コース切替を確認する。

## 6. カメラと表示

通常は胴体を画面高の60%位置へ追従表示する。操作中はカメラを固定する。右端の帯をマウスまたは1本指で動かすと壁を閲覧でき、矢印・PageUp/PageDown・Home/Endにも対応する。プレイヤー操作を始めると通常位置へ戻る。

２本指スクロールや右ドラッグによる閲覧は使わない。

画面にはレベル、2点固定の表示、スタミナ、手数／想定手数、密度情報、再開始ボタンを表示する。Undoはプレイ中のコース選択ボタンを置き換え、利用できないときは無効表示にする。試作コースの密度欄は必要ホールド数と想定手数を表示する。

「パス表示」は初期OFF。ONなら青の点線・矢印・番号で想定ルートを表示する。クリア後はピンクの実走パスを重ねる。実走パスは開始位置と各手の確定位置を結ぶ折れ線であり、スワイプ途中の軌跡は記録しない。キャンセル・同一接触位置への復帰は記録しない。再開始・次レベルでは初期化する。

クリア時は結果パネルを小さくし、背景のぼかしを解除して登頂アニメーションを見せる。クリア後も右端の帯で壁を閲覧できる。失敗時・プレイ中は実走パスを表示しない。

### 横長ホールドの生成と検証

既存・試作の両コースで、通常のホールド候補がそろった後、経路探索の前に `assignWideHolds()` を実行する。レベル由来の独立したシード乱数で追加0〜2個を選び、通常ホールドへ `wide: true` を付ける。開始3ホールドとゴールは変更しない。初期ガイド上のいずれかの胴体位置で両手または両足が届く候補を対象とする。

`canShareHold()` を前方候補評価と吸着の組み合わせ探索で共用し、実操作・`solveRoute()`・`replayRoute()` に同じ共有規則を適用する。その後の密度評価・ホールド削減は従来どおり行う。途中の横長ホールドも不要なら削除されるので、開始用1個を含め最終配置は1〜3個となる。途中の横長ホールドを2肢で使うことをクリアの必須条件にはしない。リトライでは形状・配置・想定ルートを再現する。

## 7. プレイヤーの描画

評価式・優先順・条件衝突時の扱いは [姿勢制御ドキュメント](docs/pose-control.md) を参照。

上腕・前腕は各50px、太もも・膝下は各55pxの固定長とする。肩・腰と接触位置から求めた関節候補を、足先より低い膝の数 → 全違反数 → 共通コストの順で選ぶ。胴体探索にも同じ順を使う。

- 内向きの量×4、ボディ内部への入り込み×4をコストに加え、外への開き×1を差し引く。
- 手が肩以下の場合、肘が肩を超える高さ×0.75を加える。通常は低い肘を選ぶが、近い手で外向きと両立できない場合は外向きを優先できる。従来の「肩以下」は絶対条件から評価条件へ変更する。
- 膝が足先より低い距離×4をコストに加える。1px超低い膝の数を最優先で減らす。股関節基準の膝上げ評価と120度の係数は廃止し、伸びた脚にも適用する。膝が足先以上の高さなら追加の持ち上げ報酬はない。
- 前フレームの相対関節位置からの距離×0.03を加え、ほぼ同評価の候補の切り替えを抑える。

肩・腰と接触先が完全に一致する場合は、固定長を保つ円周上の16候補を同じ式で評価する。条件を両立できなくても骨の長さと接触先は維持する。すべての手足を先に描き、ボディをその上に描く。重なった手足はボディに隠れる。描画の評価は足先の到達判定、吸着先、コース生成を変更しない。

固定された2肢に「固定」を表示し、可動側を別色で描く。可動域の共通部分を表示する。固定足がある場合は、中点の高さ制限と、高い足の内側禁止領域も表示範囲に反映する。描画の関節位置や揺れは接触・可動域判定に影響しない。

## 8. 実装と検証

`config.js` → `utils.js` → `stage.js` → `game.js` → `route.js` → `prototype.js` → `input.js` → `renderer.js` → `main.js` の順で読み込む。ビルド不要で `file://` から実行可能。

```sh
node tests/routes.cjs
node tests/prototype.cjs
node tests/poses.cjs
node tests/animations.cjs
node tests/rules.cjs
```

20レベルの通常入力再生、2点固定と可動域、生成時と実操作時の支持ペア一致、最終ゴール、スタミナ、ホールド削減の必要性、キャンセル、実走パスを確認する。探索は最大40手・幅12のビーム探索で、全レベル・全入力の網羅証明や最短手数の証明ではない。

過去の比較・操作検討は `docs/layout-study.md`、`docs/controll_test.md`、`docs/report_light.md` に履歴として残す。現行仕様と矛盾する場合は本書と実装を優先する。


## 9. 姿勢確定と登頂のアニメーション

有効な移動を確定した後、4つの接触先を保って胴体を上下左右へ調整する。関節候補と共通の評価式を使い、最初に足先より1px超低い膝の数を減らす。同数なら内向き・胴体との重なり・低い手に対する高い肘・足先より低い膝の全違反数を減らす。それも同数なら4肢の合計コストが改善する位置を選ぶ。微小な改善だけで遠くへ動かないよう移動距離の小さなコストを加える。改善が見つからなければ移動しない。

元の胴体位置から32方向・7距離を探索し、最良位置の周囲を4段階で細かく調べる。画面内の位置に限り、移動の全区間で4肢の到達距離・足の高さ・高い足の150度条件を満たすことを解析的に確認する。禁止領域を飛び越える位置は採用しない。有限の候補探索なので、到達可能領域全体の最適解を保証するものではない。

選んだ位置へ240msで移り、次の操作まで維持する。骨の長さとホールド位置は変えない。キャンセルや同じ接触位置への復帰では新たな調整を始めない。

クリア時は650msで両腕をほぼ伸ばし、脚をホールドから離して下へ伸ばす描画へ移行する。両手はゴール位置に固定したまま、そこを回転中心として体全体を約±3.2度揺らす。パス表示のON/OFFにかかわらず、結果パネルは小さく表示して動きを見せる。

どちらも描画専用の変化であり、ゲーム本体の胴体座標・接触先・実走パス・手数・勝敗を変更しない。次の操作、リトライ、次ステージでは描画状態をリセットする。検証は `node tests/animations.cjs`。
